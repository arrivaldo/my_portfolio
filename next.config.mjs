// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['pbs.twimg.com'],
      domains: ['cdn.dribbble.com'],

    },
  };
  
  export default nextConfig;
  